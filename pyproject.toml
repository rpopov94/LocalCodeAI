[project]
name = "rag-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.0.13",
    "gradio>=5.34.2",
    "ollama>=0.5.1",
    "sentence-transformers>=4.1.0",
]

[project.scripts]
start = "app:app"
